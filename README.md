# epam_react_herasymenko

To run project you need to:
- install packages in the root directory and 
  server directory (movies_server);
- run command 'npm start' in the root directory and 
  server directory.
